# -----------------------------------------------------------------------------
# Vim / Neovim aliases
# -----------------------------------------------------------------------------
if which nvim >/dev/null 2>&1; then
  alias vim=nvim
  alias vi=nvim
  alias v=nvim
else
  alias vi=vim
  alias v=vim
fi
